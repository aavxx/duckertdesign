app = "duckert-imap-worker"
primary_region = "arn"  # Stockholm — closest to Denmark

[build]
  [build.args]
    NODE_VERSION = "20"

[processes]
  app = "npm start"

[env]
  NODE_ENV = "production"

[[vm]]
  size = "shared-cpu-1x"
  memory = "256mb"
